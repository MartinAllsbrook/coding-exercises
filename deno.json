{
  "tasks": {
    "server": "cd deno-server && deno bundle main.ts --outdir ./ && deno run --allow-net --allow-read server.ts",
    "dev": "deno run --watch main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
